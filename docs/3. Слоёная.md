# 3. Слоёная архитектура (Layered)

Слоёная архитектура описана ещё у Фаулера (2002) и Эванса (2003), но в Spring она прижилась в форме Controller → Service → Repository.

## Что имеем на практике

В типовом Spring-проекте слоёная архитектура выглядит так:

```
┌─────────────────────┐
│    Presentation     │  ← @RestController, DTO
├─────────────────────┤
│    Business /       │  ← @Service, бизнес-логика
│    Application      │
├─────────────────────┤
│    Data Access      │  ← @Repository, JPA Entity
└─────────────────────┘
```

Зависимости идут **сверху вниз**: контроллер знает о сервисе, сервис знает о репозитории, репозиторий знает о БД. Принцип простой: каждый слой может вызывать только слой непосредственно под ним.

На практике это означает структуру пакетов вида:

```
com.bookstore
├── controller
│   └── BookController
├── service
│   └── BookService
├── repository
│   └── BookRepository
└── model
    └── Book
```

А вот как это выглядит в коде — упрощённо, на примере нашего магазина книг:

```java
// Presentation Layer
@RestController
@RequestMapping("/books")
public class BookController {
    private final BookService bookService;

    @PostMapping
    public BookDto create(@RequestBody CreateBookRequest request) {
        return bookService.createBook(request);
    }
}

// Business / Application Layer
@Service
@Transactional
public class BookService {
    private final BookRepository bookRepository;

    public BookDto createBook(CreateBookRequest request) {
        Book book = new Book(request.title(), request.author(), request.price());
        book = bookRepository.save(book);
        return BookDto.from(book);
    }
}

// Data Access Layer
@Repository
public interface BookRepository extends JpaRepository<Book, Long> {
    List<Book> findByAuthor(String author);
}
```

Узнали свой код? Controller → Service → Repository — три слоя, зависимости строго вниз. На первых порах этого достаточно. Но смотрите, что здесь уже происходит:

- `BookService` **напрямую** вызывает `bookRepository.save(book)` — бизнес-логика жёстко прибита гвоздями к JPA.
- `Book` (доменный объект) одновременно является `@Entity` — JPA-аннотации живут прямо на классе, который должен представлять бизнес-правила.

Это и есть главная архитектурная уязвимость слоёной модели: **инфраструктура просачивается в домен**, и мы даже не замечаем.

## Почему слоёная — самая популярная

Слоёная архитектура не самая «правильная», но она — **самая популярная**. И вот почему:

### 1. Spring Boot толкает в неё по умолчанию

Откройте `start.spring.io`, добавьте Spring Web и Spring Data JPA — и у вас уже есть всё для слоёной архитектуры. Первый же гайд на [spring.io](https://spring.io/guides) показывает Controller → Service → Repository. Фреймворк не навязывает, но **подсказывает**.

### 2. Низкий порог входа

Слоёная архитектура интуитивно понятна. Она повторяет то, как мы привыкли думать о программах: «запрос пришёл → обработали → сохранили». Не нужно разбираться в доменных моделях, портах и инверсии зависимостей — достаточно знать, что `@Service` вызывает `@Repository`.

### 3. Естественная карта для веб-запроса

Жизненный цикл HTTP-запроса идеально ложится на три слоя: приняли запрос в контроллере → обработали бизнес-правила в сервисе → сходили в базу через репозиторий. Никакого лишнего умственного усилия.

### 4. Быстрый старт для MVP и прототипов

Когда нужно «показать работающий продукт через две недели», слоёная архитектура — лучший выбор. Она не требует проектирования интерфейсов, портов, адаптеров. Пишем код — он работает.

## Когда слоёной достаточно

- **Простой CRUD и MVP.** Бизнес-логика — создать/прочитать/обновить/удалить, главное — скорость выхода.
- **Стабильный стек.** База данных никуда не денется, внешние интеграции предсказуемы.
- **Небольшая команда без амбиций на рост.** Все понимают структуру «из коробки», проект не планирует жить годами.

## Где начинаются ограничения

Проблемы не видны на старте. Они накапливаются по мере роста.

### 1. Сервис знает о БД — и обо всём внешнем мире

`BookService` напрямую зависит от `BookRepository`, а тот — от JPA/Hibernate. Бизнес-логика **привязана к способу хранения данных**. Поменять JPA на MongoDB или переехать с REST на очередь — придётся переписывать сервис.

Но настоящая засада — в двойной роли доменного объекта:

```java
@Entity
@Table(name = "books")
public class Book {
    @Id @GeneratedValue
    private Long id;
    private String title;
    private String author;
    private BigDecimal price;
    // геттеры, сеттеры...
}
```

Класс `Book` одновременно и бизнес-сущность, и отражение таблицы в БД. `@Entity`, `@Table`, `@Id` — это **инфраструктурные** аннотации про способ хранения, не про бизнес. Но живут прямо в домене.

Это как если бы паспорт содержал инструкцию для сканера: «считывать лазером, 300 dpi». К паспорту отношения не имеет — а лежит внутри. В слоёной это норма. В луковой и гексагональной — разделяют.

### 2. Сложно тестировать бизнес-логику без БД

Хотите протестировать правило «нельзя купить книгу, если её нет в наличии»? Придётся либо поднимать H2, либо мокать репозиторий. Тест привязан к тому, **как** данные попадают в сервис, а не только к тому, **какие** они.

### 3. Сервис-монстр

`BookService` начинается с `createBook()`. Потом добавляются `updateBook()`, `deleteBook()`, бизнес-правила скидок, отправка email, вызов платёжного шлюза. Через полгода — 500 строк, никто не помнит, за что он отвечает. Слоёная архитектура не даёт механизма, который **принудительно** ограничивает размер сервисов. Всё на дисциплине. А дисциплина заканчивается к третьему горящему дедлайну.

---

## Итог по слоёной

Слоёная архитектура — **хороший старт**. Простая, понятная, подкреплена экосистемой Spring. Её проблема — она **не защищает от собственного роста**. Когда проект перерастает слоёную — в игру вступают луковая и гексагональная.
